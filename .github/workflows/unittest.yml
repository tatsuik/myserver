name: unittest
on:
  push:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - shell: bash
        run: |
          export ENV=UNIT
          docker compose up --exit-code-from myserver
